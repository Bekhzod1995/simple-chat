const handles = (state, action) => {
  switch (action.type) {
    // case LOAD_CHANNELS:
    //   return {
    //     channels: action.payload,
    //   };
    default:
      return state;
  }
};

export default handles;
